# Jekyll build output and caches
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Ruby / Bundler
vendor/
.bundle/
Gemfile.lock

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Claude Code local settings (personal automation — not for the repo)
.claude/settings.local.json
supabase/.temp/
.superdesign/tmp/
.worktrees/
.superpowers/

# Local-only extracted PowerPoint media. The public lecture version uses
# code-based visuals instead of publishing source-deck images.
assets/course-materials/information-security/week-01/lecture/assets/media/
.gated-content-build/

# Claude Code launch config (local preview wiring)
.claude/launch.json

# Local-only devDependency for tools/verify-content-import-security.mjs
# (typescript, used to transpile deck-validation.ts for the Node-side test).
# Not a project-wide npm setup — no package.json is committed.
node_modules/
